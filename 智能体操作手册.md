# 智能体操作手册（Cahier 站点）

> 本手册写给后续接手本站的任何智能体（也适用于人类协作者）。
> 它记载：项目是什么、改动从哪里下手、标准工作流、质量红线与常见故障。
> **动手前先通读「零、铁律」与「一、项目地图」；交付前必须走完「六、交付前检查清单」。**
> 最后更新：2026-09-13。

---

## 零、铁律（任何任务都不许违反）

1. **先读后改**：改任何文件前先完整读它与它的上下游（插件 → 样式 → 源文档 → 构建脚本）。不允许凭文件名猜结构。
2. **只改被点名的范围**：用户没要求的内容、排版结论、批注原文一律保持原样；个人判断与改动要分开标注。
3. **图片一律 WebP**：站点所有位图素材必须是体积小的 WebP（封面、插图均如此）；不要引入 PNG/JPG 作为最终素材，必要时先转换压缩（3D 模型用 .glb，不在此列）。
4. **文字守恒**：重排三栏文档时只允许「移动、分带、合并被 PDF 撕裂的同一段落」，**不允许删改原文实质内容**；批注/讲稿正文若必须重写（如碎片修复），要在交付说明里逐条交代。
5. **必须真实构建验证**：改动后跑 `npm run build`（含 `astro check` 与站内链接校验），并用浏览器实际渲染截图核对，不允许只看代码就宣称完成。
6. **不提交构建产物**：`site/dist/`、`node_modules/` 已被忽略，部署由 GitHub Actions 在 `main` 上完成；本地只推源码。
7. **大文件红线**：GitHub 单文件硬上限 100MB；3D 模型等二进制素材入库前先看体积，超限要换压缩版或 LFS，不能硬推。
8. **不猜日期/数字/路径**：一切数字来自输入、信源或可复现计算；路径以本手册与实际目录为准。
9. **全程中文交付**，产物名要让人看得懂，最终回复必须给出文件路径与打开方式。
10. **最新永远在最前（用户排放习惯，任何模块不许违反）**：
    - **文献笔记模块**：不管哪个学科分类，分类内部的所有文本一律按时间**从新到旧**排列；
    - **教学讲稿模块**：不管哪个课程合集，合集内部的所有文本一律按时间**从新到旧**排列（专题页同理）；
    - 学科书背 / 课程卡片这类「容器导航」保持各自固定顺序（学科按门类表、课程按拼音首字母），**变的只是容器里面的文本顺序**；
    - 时效键与实现见 §1.4，排序只发生在展示层，绝不回写源 md。
11. **提交后自觉推送（用户钦定）**：验收全绿、`git commit` 完成后**直接 `git push origin main`，不用再问用户**；只有推送失败（冲突 / 认证 / 网络）才停下报告。推送即触发 Actions 部署。
12. **★★★【最高重点·用户反复强调，违反即返工】左右便签栏（L/R）只放「我自己的个人笔记」，尤其是手写笔记；严禁把 AI 生成的内容塞进左右栏 ★★★**
    - **唯一合法来源**：左/右栏只能放用户本人的东西——PDF 上的手写笔迹/批注、用户另给的便签或个人笔记转录。
    - **明令禁止**：AI 纪要/AI 生成的 `【提示】`、`【思考】`、`【核心逻辑】`、`【本节总结】`、`【演变逻辑链】`、`【核心规则】`、关键词书签、AI 摘要、AI 线索等，**都不是个人笔记，一律不得放进 L/R**（哪怕源 PDF 里就印着这些 AI 标签）。
    - **没有个人/手写笔记时，L、R 两栏一律留空**：只写空的 `<!--col:L-->` / `<!--col:R-->`，**绝不能为了“凑成三栏好看”而拿 AI 内容填侧栏**。
    - **原 PDF 里的文字（即使是 AI 生成的提示/思考/总结）全部整理进中栏 M（原文纸）**，按原文先后顺序排版，受铁律 4「文字守恒」约束，一字不丢。
    - **一句话判据**：落 L/R 前先问“这行字是不是用户亲手写、亲手标的？”——不是，就进 M，不进 L/R。
    - 先例：§9.3 社保讲稿左栏“AI 书签”被整栏清空、左栏留空；2026-09-11 土地资源管理《9月7日讲稿》因把 AI 的提示/思考/总结误放左右栏而整页重做为纯中栏。
13. **★【重点·用户明确】文献书架「只有学科分类，没有合集」★**
    - **文献笔记模块的层级只有一层：12 个学科分类（书背）→ 分类下直接是单篇文献卡片**。即便一批文献同主题/同作者/同课程，也**严禁**在书架或 `文献笔记\` 目录下再建“XX 主题合集”“XX 论文集”之类的子合集/子文件夹。
    - 多篇同主题文献的正确做法：每篇一个 md、**平铺**在 `文献笔记\` 根目录，各自用 frontmatter 的 `category`（如 `"11管理学"`）归到对应学科分类即可；`covers\`、`figures\` 只是图片资产目录，不是合集。
    - 对比：**教学讲稿模块才有“课程合集”**（顶层课程文件夹 → 合集页）。两个模块的容器形态不同，不要把“合集”概念搬到文献书架。

---

## 一、项目地图

### 1.1 这是什么

Cahier 是一个 **Astro 7 静态站点**，形态是「活在网页上的康奈尔笔记本」：

- **文献笔记**：中栏按学术论文 PDF 视觉规范排版（刊头、居中标题、作者、内容提要、`〔n〕`脚注、悬挂页脚注）；左右两栏是便签纸，**只放用户本人的手写批注/个人笔记（铁律 12），没有就留空，绝不放 AI 生成内容**。
- **教学讲稿/草稿**：按学科分目录（社会保障学、宪法、德语、决策理论……），同样走三栏文档体。
- **3D 书桌**：「文献笔记」模块初始页顶部的可交互 3D 场景（一张 L 型转角书桌，OrbitControls 环绕查看、缓慢自转），白昼/暗夜随整站主题联动。
- **暗夜 / 白昼主题**：右上角药丸按钮一键切换，白昼=钴蓝（cobalt）、暗夜=深夜墨蓝（night），用「圆形破口」clip-path 扩散转场，选择存 localStorage 并在渲染前应用（防刷新白闪）。
- 本站**已无游戏模块**（恐龙快跑及其 Python 源码、素材、音乐已于第五轮彻底删除），不要再恢复任何 `/game` 路由或游戏入口。

### 1.2 顶层目录

```text
Cahier-main/
├─ site/                      # Astro 站点本体（所有代码改动都在这里面）
│  ├─ src/
│  │  ├─ pages/               # 路由：index / notes/[id] / collections/[id] / 404
│  │  ├─ components/modules/  # 首页各模块（LiteratureModule = 文献书架 + 3D）
│  │  ├─ plugins/             # Markdown 内容管线（核心！）
│  │  ├─ styles/              # global.css 主题变量 / folio.css 三栏文档体 / modules.css
│  │  ├─ lib/catalog.ts       # 内容收集：glob 根目录全部 md（排除 site 等）
│  │  └─ content.config.ts    # 内容集合 schema（frontmatter 字段定义）
│  ├─ public/                 # 原样拷贝到站点根的静态资源
│  │  ├─ 3d/                  # 3D 书桌：desk.html / desk.js / desk.glb（约 1.6MB）
│  │  └─ media/               # sync-assets 从根目录同步来的 md 配图（自动生成，勿手改）
│  ├─ scripts/                # 构建辅助脚本（sync-assets / check-built-site / 3d 打包源码在 scripts/3d）
│  ├─ astro.config.mjs        # remark 插件注册顺序、站点地址
│  └─ package.json            # build = sync-assets → astro check → astro build → 校验
├─ 文献笔记/、社会保障学/、宪法/… # 内容源 Markdown（被 catalog.ts glob 进站点）
├─ _tools/                    # 一次性内容治理脚本（cjs，处理 md 用，不参与站点构建）
├─ .github/workflows/deploy.yml # push main → Actions 构建 → GitHub Pages（自定义域名 20061018.xyz）
└─ README.md
```

### 1.3 内容管线（最重要的一条链）

`astro.config.mjs` 中对 Markdown 依次执行：

1. `remark-math`：公式；
2. `remark-obsidian`（`src/plugins/remark-obsidian.mjs`）：`> [!hand]` / `> [!question]` 等 Obsidian callout → `<aside class="callout">`；`[[wikilink]]` → 站内链接；
3. `remark-three-column`（`src/plugins/remark-three-column.mjs`）：把 `<!--folio-->` / `<!--col-->` / `<!--band-->` 注释转成结构化容器：

```html
<section class="folio" data-page="第 N 页">
  <div class="folio__band">            <!-- 一带 = 一行三栏，带内顶部对齐 -->
    <div class="folio__col folio__col--left">…左注…</div>
    <div class="folio__col folio__col--main">…原文…</div>
    <div class="folio__col folio__col--right">…右批…</div>
  </div>
  <!-- 更多 band -->
  <p class="folio__pageno">第 N 页</p>
</section>
```

样式在 `src/styles/folio.css`：`.folio` 纵向 flex；`.folio__band` 是 `1fr 2fr 1fr` 三栏 grid；`.doc-body` 用两条 25%/75% 竖线背景模拟连贯便签纸；≤920px 时每带单列堆叠（原文 → 左注 → 右注）。

### 1.4 排序口径：容器内文本「最新在最前」（对应铁律 10）

- 统一时效键在 `src/lib/catalog.ts` 的 `noteRecencyKey(data)`：**frontmatter `date`（讲稿日期）→ 微信读书 `lastReadDate` → `finishedDate` → `readingDate` → `publishTime`**，取第一个非空值并截成 `YYYY-MM-DD`；都没有则为空串。
- 比较器 `compareNotesRecent`：时效键**降序**（新→旧，日期串可直接字符串比较），**无日期条目沉到最后**；同日 / 都无日期时按标题拼音升序兜底，保证顺序稳定可复现。
- 作用点（改排序时这三处要一起看）：
  1. `catalog.ts`：全局 `notes`、每个合集 `collection.notes`、每个专题 `topic.notes`（→ 教学讲稿各课程合集页 / 专题页的文本顺序）；
  2. `src/lib/bookshelf.ts`：文献书架**每个学科分类内部**与「全部」视图（`getBookshelf`）；
  3. `src/components/modules/LecturesModule.astro`：搜索「相关讲稿」结果索引同样最新优先。
- **不动的部分**：文献学科书背顺序（`BOOKSHELF_CATEGORY_ORDER`）、教学讲稿课程卡片的拼音首字母分组、合集 / 专题自身的导航顺序。
- 新增文本只要 frontmatter 写了 `date`（schema 已支持 `date` 字段）就会自动归位；**不要为了排序去改 md 文件名或正文**。

---

## 二、标准工作流 A：新增/修改一篇三栏文档

### 2.1 frontmatter（字段以 `src/content.config.ts` 为准）

```yaml
---
doc_type: lecture          # lecture 讲稿 | literaturenote 文献 | draft 草稿
title: "9月2日讲稿"
course: "社会保障学"         # 讲稿用
lecturer: "贾玉娇老师"
date: 2025-09-02
discipline: "社会保障学"
category: "05公共管理"       # 书架书背按 category 分类
cover: "./covers/xxx-960.webp"   # 必须 webp
---
```

### 2.2 三栏标记语法（写在正文）

```text
<!--folio:第1页-->          开始一页，冒号后是页脚显示的页码文字

<!--col:L-->               左栏：只放「我自己的个人笔记/手写批注」（见铁律12，没有就留空）
<!--col:M-->               中栏：原文/讲稿正文（唯一的“主内容”，原PDF的AI提示/总结也进这里）
<!--col:R-->               右栏：只放「我自己的个人笔记/手写批注」（见铁律12，没有就留空）

<!--band-->                分带：在同一页内新开一条三栏对齐带（详见工作流 B）

<!--/folio-->              结束本页
```

规则：

- 一页内 L/M/R 可省略（空栏会自动补空白 div，保持中栏永远居中），但**顺序必须是 L → M → R**；每带都要重新声明 `<!--col:…-->`。
- **★铁律12再强调★：L/R 便签纸只放用户本人的手写/个人笔记；AI 生成的【提示】【思考】【核心逻辑】【本节总结】等一律进 M、绝不进 L/R。源文档没有个人笔记时，L/R 就空着（写空标记即可），只整理中栏原文纸。**
- 标记必须**独占一行**；插件会把混在 HTML 节点里的边界注释拆开，但不要依赖这个容错。
- 跨页续段：上一页中栏结尾若没有句末标点（。！？…：；）等，插件会自动给本页首段加 `.para-cont`（取消首行缩进）。不要手工加。
- 中栏可用的排版件：`<p class="doc-kicker/doc-editornote/doc-title/doc-author/doc-lede/doc-keywords/doc-section">`、`<mark>` 高亮、`<u>` 下划线、`<sup class="fn-ref">〔n〕</sup>` 脚注号、页脚 `<div class="folio-footnotes">…<p class="fn">…</p></div>`。
- **★讲稿正文开头不要用 `>` blockquote 写课程标题/说明（2026-09-13 用户打回）**：中栏 `blockquote` 在 `folio.css` 中被设为 `font-size: 0.84em` 的小一号灰字，专门用于「参会人/时间地点」题注；讲稿第一页开头的课程名、授课教师等行直接写普通正文段落即可，不要加 `>`。正文中间真正需要引用/题注的内容才用 blockquote。
- 左右栏 callout：
  ```markdown
  > [!hand] 手写批注
  > *（位置/呼应对象说明）*
  > 批注正文
  >
  > [!question] 引导问题
  > 1. 问题一
  ```

### 2.3 数学公式与英文摘要排版（★重点，2026-09-11 因此返工）

**A. 数学公式必须走 KaTeX，严禁用 `<sub>/<sup>` + Unicode 硬拼**

站点公式链是 `remark-math` + `rehype-katex`（KaTeX），**只有写成 LaTeX 才会被渲染成公式**；用 `x′<sub>ij</sub>`、`x̄`、`Σ`、`S²`、拿 `/` 当分数线这类“伪公式”不会被 KaTeX 接管，页面上就是一团没排版的源码（用户明确打回）。

- **行内**变量/符号一律 `$...$`：`$x_{ij}$`、`$C_1$`、`$\bar X$`、`$S^2$`、`$1\le i\le n$`；不要再用 `<sub>` 排下标。注意：作者单位上标 `<sup>1,2</sup>` 是脚注不是公式，保留 HTML。
- **独立公式**独占一行、上下空行，用 `$$ … $$`，编号用 KaTeX 的 `\tag{n}`，与原文编号一一对应：

  ```text
  $$
  x'_{ij}=\frac{x_{ij}-\min\{x_{ij}\}}{\max\{x_{ij}\}-\min\{x_{ij}\}}\tag{1}
  $$
  ```

- 分数线用 `\frac{}{}`（多层分式里加 `\displaystyle`）、求和 `\sum_{i=1}^{n}`、均值 `\bar x`、根号 `\sqrt{}`、希腊字母 `\rho \beta \varepsilon \gamma \lambda \mu`、省略号 `\cdots`、公式里的英文词用 `\text{...}`。
- 一篇内公式编号连续且与原文一致；“式中：”解释行里出现的**每个符号也要包成行内 `$...$`**。
- **提交前先用 KaTeX 逐条试渲染**：在 `site/` 写个临时脚本，对该 md 抽出的每个 `$$…$$`（displayMode）和 `$…$`（inline）调 `katex.renderToString(tex,{throwOnError:true})`，必须 **0 FAIL**，再跑 `npm run build`。

**B. 英文 Abstract 块：正体/斜体分行、按语义分段，禁止糊成一个 `<p>`**

- 英文**标题（粗体正体）、作者（正体）、单位（正体）各占一段**；只有摘要正文用 `<em>…</em>` 斜体；`Abstract：`/`Keywords：` 标签正体加粗，关键词正体并独立成段。**严禁**把“标题+作者+单位+斜体摘要+关键词”全塞进同一个 `<p>`（正体斜体会糊成一团、无法换行，用户明确打回）。
- 斜体摘要按原文语义**分段、段间空一行**：背景 / 数据与方法 / 结果 (1)(2)(3) 各点 / 结论，每条一个 `<p><em>…</em></p>`，不要一整坨长 `<em>`。
- **英文摘要同样受铁律 4 文字守恒约束，逐句对照原 PDF，不许“压缩”、不许漏句**（本次事故：部门合作型那篇摘要被漏掉 “For the Public to Public / Public to Private / Private to Private mode…” 三个整句）。双栏 PDF 的跨行断词（`de- velopment`、`geo- economy`）要拼回完整单词。

**C. 工具教训：PowerShell 内联 Python 不要带 `$`**

PowerShell 双引号会把 `$C_` 当成它自己的变量先展开、静默吃掉；凡是正则或替换串里含 `$`（LaTeX、`$...$` 都算），**一律写成 `.py` 脚本文件再执行**，不要图省事用 `python -c "..."` 内联。

**D. 中文标点规范（★2026-09-13 用户要求“从脚趾头到头发”地毯式清查，务必逐条遵守）**

总原则：**中文行文用全角中文标点；半角标点只在外文原文、数学公式、代码、参考文献著录等“非中文行文”场景保留。**

1. **引号（重灾区，用户两次打回）**
   - 中文行文引用词语/概念/句子，一律用中文弯引号：双引号 `“”`（U+201C/U+201D），嵌套时内层用单引号 `‘’`（U+2018/U+2019）。**严禁**在中文里用英文直引号 `"`（U+0022）/`'`（U+0027）。
   - 引号必须左右配对、方向正确：先左 `“` 后右 `”`，不得全用右引号、不得 `""` 连用、不得漏左引号。
   - **保留半角的例外**：① frontmatter YAML 语法的 `"…"`；② HTML 标签属性 `class="…"`；③ 整句外文原文引用（如 `"Little Prince! …"`）；④ 外语撇号/省音（l'homme、it's、don't）与英语音标重音（/ɪ'nɔ:məs/）；⑤ 数学转置 $x'$。
2. **句末点号与停顿**：中文句子的问号 `？`、感叹号 `！`、逗号 `，`、顿号 `、`、分号 `；`、冒号 `：`、句号 `。` 一律全角；中文行文省略用六点省略号 `……`，不用三个英文句点 `...`。
3. **括号**：中文行文里的序号与夹注用全角 `（）`，如 `（1）（2）`、`世界贸易组织（WTO）`、`框定（framing）`。
   - **保留半角 `()` 的例外**：① 数学/统计公式与函数 `(1−a)`、`f(x)`、`N(0,1)`、半开区间 `[0.75, 0.85)`；② 整句外文原文及其内部语法标注（`a box (that) you gave me` 表可省略、`not only...but (also)...`）；③ 外语词形变化标注（德语 `-(e)n`）；④ 英文术语对照表的英文列与其缩写 `Photovoltaic (PV)`；⑤ 法条/文献编号内层 `C/10:1(12)`。
4. **参考文献著录（GB/T 7714）整体保留半角**：作者分隔 `韩璟, 陈泽秀`、出版地 `武汉: 出版社`、年卷期 `2020, 42(9): 1715-1727`、`(eds.)`、`(2007)`、`p.59` 等，以及“引用格式”行，**不做全角化**；脚注 `<p class="fn">` 内的英文文献同样保留。
5. **判定顺序**：先剥离 frontmatter / 代码块 / 公式 `$…$` / 音标 `/…/ ` / HTML 标签 / markdown 图片 `![](…)` / 脚注与参考文献区，再对剩余中文行文做全角化；含 BOM 的文件先剥 `﻿` 再定位 frontmatter，且写回时保留原 BOM 与原换行风格（CRLF/LF）。

### 2.4 配图与封面

1. 图片放该学科目录的 `figures/`、`covers/` 子目录；
2. **必须 WebP**：封面建议 960px 宽，插图控制在几百 KB 内；
3. 构建时 `scripts/sync-assets.mjs` 会把根目录 md 引用到的素材按相对路径镜像进 `site/public/media/…`，所以 md 里一律写**相对路径**（`./figures/x.webp`），不要写 `/media/...` 绝对路径；
4. 缺封面时可参考 `scripts/generate-missing-covers.mjs` 的生成方式，风格要与同学科已有封面成套，不要批量味。

### 2.5 操作步骤

1. 找到对应学科目录的 md（没有就新建，frontmatter 齐全）；
2. 按 PDF 页逐页写 folio，原文进 M、批注进 L/R；
3. 需要垂直对齐处按工作流 B 加 band；公式按 §2.3-A 走 KaTeX、英文摘要按 §2.3-B 分行分段；
4. 运行审计（见 6.2），公式先过 KaTeX 试渲染，再 `npm run build`；
5. `npm run preview` + 浏览器截图核对三栏位置、公式渲染、英文摘要换行与移动端堆叠。

---

## 三、标准工作流 B：左右批注与原文「垂直高度对齐」（band 机制）

### 3.1 什么时候需要 band

用户的核心诉求：**批注不要一律堆在每页左上角，要和它呼应的原文处在差不多的水平线上。** 当一页中栏有多个段落/小节，而左右栏有多条分别呼应不同段落的批注时，就必须分带。

### 3.2 怎么分带

1. 先读中栏，按「批注呼应点」把中栏切成若干连续片段；
2. 每条批注放进其呼应片段所在的那一带；
3. 一带内仍按 `L → M → R` 写，带与带之间插一行 `<!--band-->`；
4. 若呼应点落在**同一个长自然段中间**：把该段在呼应句末切开，第一段保持普通段落，后续片段包成
   `<p class="para-run">…</p>`（无首行缩进、无段距，视觉上仍是同一段）。

示意：

```text
<!--folio:第2页-->
<!--col:L-->（本带左注，可空）
<!--col:M-->…原文片段 1…
<!--col:R-->呼应片段 1 的批注
<!--band-->
<!--col:L-->
<!--col:M--><p class="para-run">…同段后续片段 2…</p>
<!--col:R-->呼应片段 2 的批注
<!--/folio-->
```

### 3.3 切句注意

- 切点在**句末**（。！？，或英文句点且其后紧跟 `<sup>` 脚注）；
- 句末若跟着收束引号（”』）、`<sup class="fn-ref">…</sup>`、行内闭合标签（`</u></mark>`），要一并留在上一片段尾部，避免把闭合标签甩到下一带造成错配；
- 一次重排后做**守恒校验**：中栏去空白后文字必须与原文逐字相等；左右栏块集合必须不多不少（只允许换位置）。批量重排建议写一次性迁移脚本（参考本次 `migrate-law-bands` 的做法：锚点切句 + 中栏顺序守恒 + 侧栏块多重集守恒），不要手工搬 10 万字体量的文档。

### 3.4 常见错误（本次修复前的真实事故）

- **左栏碎片/原文串行**：把 OCR 切碎的中栏原文塞进 L，出现 `- 年金融危机后…`、`- 低于社会最低标准；1834 • 初始阶段（19` 这种半句、内联 `•`、跨页续词、与中栏大段重复的“断层”内容。
  - 左栏正确形态是**稀疏的关键词/线索提示**（参考决策理论、知产法等干净讲稿），不是原文复制。
  - 中栏同类事故：一个 bullet 被切成 `- …（如深圳` / `#### 5:1）…` / `政转移支付…` 三段，甚至碎片变成 `####` 标题；跨页 bullet 被切成两条。修复时要把它们接回完整句子。
- **分带后段距异常**：首/末带的外边距清零只对整页首带、末带生效；中间带保留正常段距（folio.css 已处理，改样式时别回退）。

---

## 四、标准工作流 C：替换/维护 3D 场景

### 4.1 现状（第五轮，2026-09-07 起）

- 场景文件：`site/public/3d/desk.glb`（由 `Desk.skp` 转换的 glTF 2.0 二进制，**源是毫米单位**，viewer 内 ×0.001 归一为米，约 1.6MB）；
- 承载页：`site/public/3d/desk.html`（白昼浅天蓝/暗夜深蓝灰渐变底、加载/失败遮罩、操作提示，自带首帧主题对齐脚本）；
- 逻辑：`site/public/3d/desk.js` 是**构建产物**，源码在 `site/scripts/3d/desk-viewer.mjs`；
- 引用处：`src/components/modules/LiteratureModule.astro` 的 iframe：`withBase("3d/desk.html") + "#cobalt"`（`loading="lazy"`，不在首屏时延迟加载）。
- 历史：residence-smith(.dae) → bedroom(.glb,33MB 室内第一人称) → **desk(.glb,1.6MB 外置物体环绕)**。旧卧室因体积过大、加载慢被移除。

### 4.2 交互设计（不要改坏）

- 书桌是**外置物体**，用 `OrbitControls` 环绕：相机 3/4 俯视、`enablePan=false`、俯仰夹在 12°–84°（不翻到桌底）、限缩放、阻尼惯性；
- **缓慢自转**（autoRotateSpeed≈0.55），用户 `start` 拖拽时暂停、`end` 后约 2.6s 恢复；
- 毫米模型 ×0.001 后水平居中、底部落在 y=0；按整体跨度 span 自动定相机距离/裁剪面/阴影范围；
- 柔和接触阴影：透明 `ShadowMaterial` 平面 + 主方向光投影；
- iframe 不可见时（`visibilitychange`）暂停渲染省电。

### 4.3 把 SketchUp（.skp）模型转成网页可用的 .glb（本轮趟通的路，重点看）

本机**没有** SketchUp / Blender / assimp，SketchUp 2021+ 是私有 VFF/protobuf 封装，手写 gunzip/inflate 解不开，在线转换要上传不可控。**正解是纯 Python 的 `openskp`（无需安装 SketchUp、无需 SDK）**：

```powershell
# 注意：要用装了 openskp 的那个解释器（本机 python 命令默认指向 DoubaoWork 沙箱解释器，没装包；
# openskp 装在 pythoncore-3.14）。先 python -m pip install openskp scipy（trimesh 转顶点色需要 scipy）
& "C:\Users\NewtN\AppData\Local\Python\pythoncore-3.14-64\python.exe" your_script.py
```

转换 + 改色脚本要点（成品见 `_tools/desk_build.py`）：

1. `SkpFile.open(path).parse()` 后 `openskp.export.glb.export(skp, "raw.glb")` 导原始 glb；
2. **改色不要直接改 `model.materials[i].color`——颜色按面烘焙、导不出**。用 trimesh 加载 raw.glb，它按材质把网格拆成多个 geometry，**名字里带原始 RGB**（如 `..._Layer0_142_108_78_...` = 榉木色 rgb(142,108,78)）；按名字解析原色、命中改色表后给每个 mesh 重设 `ColorVisuals(face_colors=...)`，再 `scene.export("desk.glb")`；
3. 导出后**务必 trimesh 回读校验每个 mesh 的 face_colors**，确认改色真的写进去了；
4. openskp 导出时可能同目录写 `*_metadata.json / *_thumbnail.png` 附属物，网页不引用，删掉；
5. glTF 顶点色在 three 里要显式开：viewer traverse 时 `if (geometry.attributes.color) material.vertexColors = true`。

### 4.4 打包与验证

1. 改完 `desk-viewer.mjs` 必须**重新打包**（html 引用的是 bundle）：
   ```powershell
   cd site
   .\node_modules\.bin\esbuild.cmd scripts/3d/desk-viewer.mjs --bundle --format=iife --target=es2020 --minify --outfile=public/3d/desk.js
   ```
2. **自包含原则**：bundle 必须 IIFE 单文件、无运行时 CDN 外链；`three` 只作开发依赖；
3. 灯光/背景分白昼 cobalt、暗夜 night 两套（viewer 内 `THEMES`），并监听父页面 `postMessage({type:'le-theme'})` 与初始化时读 `parent` 的 `data-theme` 做联动；**夜间灯光不能压太暗**，否则浅色模型会变成黑块（本轮 night 提过一次亮度）；
4. 无头浏览器验证（Windows）：`chrome --headless=new --user-data-dir=<独立目录> --screenshot=... <url>`（**必须独立 user-data-dir**，否则可能静默失败；WebGL 必要时加 `--enable-unsafe-swiftshader`）；3D 首帧要在 XHR+解析编译后真实等待，优先 CDP，并 `Network.setCacheDisabled` 避免吃旧 bundle 缓存。要分别看白昼、夜间、拖拽、触屏尺寸。

---

## 五、标准工作流 D：主题、样式与构建部署

### 5.1 主题色卡与暗夜/白昼切换

- 全局 CSS 变量在 `src/styles/global.css`：`:root,[data-theme="cobalt"]` 是白昼钴蓝（HSL 三元组原始变量 + 语义别名），**`[data-theme="night"]` 是暗夜（深夜墨蓝、护眼柔白、天蓝青强调；不用纯黑、不偏靛紫）**。新增/调整颜色只改这套原始变量，全站语义色（--card/--fg/--border/--sidebar-* 等）会自动跟随；
- 切换机制在 `src/layouts/BaseLayout.astro`：右上角 `.theme-toggle`（月亮/太阳两枚图标 + 文案），点击走 `.theme-veil` **圆形破口**：以按钮为圆心 `clip-path: circle(0)→circle(maxR)` 扩散一层目标主题底色，扩散到 55%（整屏被盖住、看不到硬切）时才真正改 `data-theme`，结束移除揭示层；选择存 `localStorage["cahier-theme"]`，head 里有一段 `is:inline` IIFE 在**首帧渲染前**读存储设 data-theme，防暗夜刷新白闪；同步刷新 `meta[name=theme-color]` 并向所有 iframe `postMessage({type:'le-theme',theme})`；
- **文档纸（folio.css）的颜色是硬编码的，不跟 CSS 变量**：白昼是用户锁定的纯白纸；暗夜必须在 folio.css 末尾用 `[data-theme="night"] …` 单独覆盖（纸面转 #141a26 夜读纸、墨色文字转柔白、竖线/分页虚线/表格线降亮、公式块与隔行转深）。**踩过的坑：白昼对左右栏 callout 的 p/li/strong 显式设过墨色，夜间要左右栏都逐条覆盖，漏了左栏就会出现深底黑字；编者按 `.doc-editornote`、左右栏 `.struct-chip`、关键词边框也都是硬编码，容易漏**；
- 3D 场景用自己的十六进制色板（viewer 内 `THEMES`，见 4.4），与全局色值改动时互相对齐；
- 不要引入大面积靛/紫色系；保持“安静学院风”，白昼纯白纸面、暗夜柔和护眼。

### 5.2 常用命令（在 `site/` 下）

```powershell
npm install            # 装依赖（Node 建议 22+；engines 写 24，22 亦可构建）
npm run dev            # 开发服务器 http://localhost:4321
npm run build          # sync-assets → astro check → astro build → 链接/资源校验
npm run preview        # 本地预览 dist/
```

> **双锁文件红线（踩过 CI 坑）**：`site/` 同时跟踪 `package-lock.json` 与 `pnpm-lock.yaml`；GitHub Actions 的 `withastro/action` 优先识别 **pnpm-lock.yaml** 并以 `--frozen-lockfile` 安装。新增/升级任何依赖后，两个锁文件都要更新并一起提交，否则 CI 直接 `ERR_PNPM_OUTDATED_LOCKFILE` 失败（本地 npm 构建全绿也发现不了）。补 pnpm 锁：`corepack pnpm@11.9.0 install --lockfile-only`（只改锁文件、不动 node_modules），随后确认 `git diff pnpm-lock.yaml` 只含目标依赖条目。

### 5.3 内容审计（改任何 md 后都跑）

- 三栏结构/碎片审计：对全部 md 解析 folio/col，检查 L/R 是否含内联 `•`、过短碎片、与中栏 12 字以上重复、跨页续词开头，以及中栏 bullet 被标题切断（本次审计脚本逻辑可在会话工作区找到，需要时固化进 `_tools/`）；
- 审计必须做到**全量 0 疑点**才算完，不要只修被点名的那一篇。

### 5.4 部署（SSH → GitHub）

1. `git status` 看清改动面；`git diff` 自查；
2. `npm run build` 全绿；浏览器截图验证通过；
3. 提交信息用中文写清「修了什么、为什么」；
4. 远程走 **SSH**（`git@github.com:...`）；直接推 `main`，**提交后无需询问、自觉 `git push origin main`**（铁律 11），Actions（`.github/workflows/deploy.yml`）会自动构建发布到 Pages；
5. 推完看 Actions 是否绿；自定义域名 CNAME 在 `site/public/CNAME`，别删。

---

## 六、交付前检查清单（逐项打勾，缺一项不算完成）

- [ ] 需求点名的每个问题都有对应改动，且没有越界改其他内容；
- [ ] 全部三栏 md 通过结构审计（0 碎片/0 串行/0 错切标题）；
- [ ] 重排文档做过文字守恒核对（中栏一字不丢，侧栏块不丢不重）；
- [ ] 批注在视觉上与呼应原文同高（桌面三栏 + 移动端单列都看过）；
- [ ] 新增图片全部 WebP 且体积合理；引用为相对路径；
- [ ] 3D：bundle 已重打、自包含、书桌居中落地、拖拽环绕/滚轮缩放/自转正常、白昼明媚夜间清晰、灯光背景跟主题；
- [ ] 暗夜/白昼：圆形破口切换顺滑、刷新后主题保持不白闪、文献/讲稿三栏夜间纸面无深底黑字、3D iframe 同步；
- [ ] 没有遗留任何游戏（/game、恐龙 py、images/bgm）入口或死链；
- [ ] `npm run build` 成功（astro check 0 错、29+ 页面链接校验通过）；
- [ ] **排序符合铁律 10**：抽查文献书架分类与教学讲稿合集页，内部文本最新在最前、无日期沉底、容器导航顺序未被改动；
- [ ] 无头浏览器真实截图核对过关键页面（首屏、问题页、3D）；
- [ ] 没有把 dist/node_modules/临时脚本/截图塞进提交；
- [ ] 提交说明清楚，**已自觉 `git push origin main`（无需再问用户）**，SSH 推送成功，Actions 变绿；
- [ ] 最终回复列出：改了哪些文件、路径、验证方式与结果、遗留问题（若有）。

---

## 七、常见故障排查

| 现象 | 可能原因 | 处理 |
| --- | --- | --- |
| 左/右栏内容全堆在页首 | 没写 `<!--band-->`，整页只有一条带 | 按呼应点分带（工作流 B） |
| 同段被切开后出现缩进/段距 | 后续片段没用 `para-run` | 包 `<p class="para-run">` |
| 切句后下一片段开头是 `</u>` | 句末闭合标签没跟上片段 | 切点吃掉行内闭合标签与脚注 sup |
| 构建报 folio 结构错 | 标记没独占一行 / folio 未闭合 / band 出现在 folio 外 | 修正注释；插件对未闭合 folio 会自动收口但别依赖 |
| 页面里出现 `####` 小标题碎片 | OCR 把半句误当标题 | 并回原 bullet/段落 |
| 3D 黑屏/停在 100% | 首帧未出就截图；bundle 旧缓存；模型包围盒异常 | CDP 真实等待；禁缓存；查 GLB 世界包围盒 |
| 3D 相机埋进家具/悬在半空 | 直接用几何中心 y | 水平居中 + 站立视高（见 viewer） |
| 截图命令静默失败 | 没给独立 `--user-data-dir` | 每次换独立目录 |
| `check-built-site` 报红 | 站内链接/资源路径拼错、素材没同步 | md 用相对路径；重跑 sync-assets（build 会自动跑） |
| CI `ERR_PNPM_OUTDATED_LOCKFILE` | 只更新了 package-lock.json，pnpm-lock.yaml 漏更 | `corepack pnpm@11.9.0 install --lockfile-only` 后两个锁一起提交 |
| Git 推送被拒 | 远程有新提交 / 用了 HTTPS | 先 pull --rebase；远程改回 SSH URL |
| 文件超 100MB 推不上去 | 大模型/视频入库 | 压缩、LFS 或改走外链，不要反复重试硬推 |
| .skp 打不开/转不出 | 2021+ 是私有 VFF 封装，标准解压解不开；本机无 SketchUp/Blender | 用纯 Python `openskp` 解析导出 glb（见 4.3），不要手写逆向压缩流 |
| `import openskp` 报 ModuleNotFound | `python` 默认指向 DoubaoWork 沙箱解释器，包却装在 pythoncore-3.14 | 用 `C:\Users\NewtN\AppData\Local\Python\pythoncore-3.14-64\python.exe` 显式调用；trimesh 面色转顶点色还要装 scipy |
| 改了材质色导出 glb 却没变 | 颜色按面烘焙，改 material 定义不传导 | trimesh 按 mesh 名里的原始 RGB 分组重设 face_colors 再导出（见 4.3） |
| 暗夜下某处文字发黑看不清 | folio.css 硬编码墨色没在 `[data-theme=night]` 覆盖（左栏/编者按/结构签最易漏） | 用 DevTools 查 computed color，补夜间覆盖；左右栏 callout 子元素要左右都覆盖 |
| 夜间 3D 模型变成黑块 | 夜间灯光/exposure 压太暗，浅色模型也被压黑 | 提高 night 的 hemi/key 强度与 exposure，靠深色背景而非压黑模型来形成对比 |
| 圆形破口切换时看到硬切 | 切 data-theme 的时机早于圆铺满 | 在动画约 55%（整屏被 veil 盖住）时才 applyTheme |
| 封面卡片被撑得又大又突兀 | frontmatter `description` 中含 `inhabit/enormous/explorer/...` 这类斜线连接的长单词，默认不在 `/` 处换行，单行溢出撑大卡片 | ①`.entry-card__description` 已加 `overflow-wrap: anywhere` + `word-break: break-word` 兜底；②写 description 时斜线前后加空格（`inhabit / enormous / ...`），从根源避免长串；③注意别把单词写重复（如 geographer 出现两次） |
| 中文里出现英文直引号 `"`、引号方向全反/不配对 | 转录或写作时误用半角直引号、或左右弯引号混用 | 按 §2.3-D：中文行文统一 `“”`/嵌套 `‘’`，脚本按“含 CJK 行内直引号奇偶配对”批量替换；frontmatter、HTML 属性、整句外文原文、外语撇号/音标保留半角 |
| 中文句子里夹半角逗号/问号/括号 | 英文输入法下写作、或从 PDF/网页转录带入半角标点 | 中文行文全角化（`，？（）……`）；但数学公式、整句外文原文、GB/T 7714 参考文献著录、外语词法标注保留半角，判定顺序见 §2.3-D5 |

---

## 八、本次（2026-09-07）变更记录，供后续理解上下文

1. **三栏垂直对齐机制**：`remark-three-column.mjs` 新增 `<!--band-->` 分带；`folio.css` 由「单页 grid」改为「页内多带 grid」，新增 `p.para-run` 同段续行、移动端按带堆叠。
2. **法学文献**（`文献笔记/全球知识产权治理博弈…邵科.md`）：21 页重排为 44 带，`0 提要 / 1 前言 / 2 理念力量 / 3 话语构造…` 等结构签与全部手写批注按原文呼应锚点落位；顺带合并了 P2/P3/P9/P13/P15/P16 被 PDF 撕裂的同段。原始版本备份在会话工作区 `law-paper.backup.md`。
3. **社会保障学两份讲稿**（8月31日、9月2日）：清除左栏全部 OCR 碎片与重复原文，重建为稀疏线索提示并按章节分带；修复中栏 bullet 被 `####` 切断（如深圳 5:1 抚养比）、跨页 bullet 撕裂、游离的“潜在风险”标题等。全量审计 16 份 folio 文档清零。
4. **3D 换模**：移除 residence-smith（dae/旧 bundle/旧 html），接入 `Bedroom_Web.glb`（太空主题卧室）；新查看器为室内中心球形环顾、钴蓝灯光、八主题联动、触屏/键盘可用；源码 `site/scripts/3d/bedroom-viewer.mjs`，esbuild 打包到 `public/3d/bedroom.js`。
5. 交付前构建：`astro check` + `astro build` + 29 页面链接校验全绿，关键页面与 3D 均有截图验证；首次推送后 CI 因 pnpm-lock.yaml 未同步 `three` 而失败，已用 `corepack pnpm install --lockfile-only` 补齐双锁文件后复推。
6. **第五轮（性能瘦身 + 删游戏 + 暗夜主题 + 换书桌）**：
   - **彻底删除游戏模块**：移除 `site/public/game/`（约 4.7MB）、根目录三份「恐龙快跑」Python 源码、根 `images/`（游戏素材副本）、`bgm/`（音乐）；清掉 BaseLayout 的游戏 overlay/手柄逻辑、global.css 的 `.gamepad-portal/.game-overlay/.is-game-*` 样式、content.config 的 `!game/!images/!bgm` 排除。本站此后无游戏。
   - **暗夜/白昼主题**：global.css 新增 `[data-theme=night]` 全套 HSL 变量；BaseLayout 用主题切换药丸替换手柄按钮，圆形破口（clip-path）扩散转场 + localStorage 持久化 + head 防白闪 + 向 iframe 广播；folio.css 末尾新增夜间「夜读纸」覆盖块。
   - **3D 卧室→书桌**：删除 33MB 的 bedroom.glb（加载慢元凶）与 bedroom 三件套；用 `openskp` 把 `Desk.skp`（SketchUp 2021）转成 glb，trimesh 把榉木巧克力色/两处深蓝改成天蓝·天青（金属与白色保留），新 viewer 改 OrbitControls 环绕 + 缓慢自转 + 接触阴影 + 白昼/夜间双灯光。最终 desk.glb 仅约 1.6MB，3D 体积降约 95%；iframe 改 lazy。
   - 性能合计：游戏约 9.2MB + 卧室约 33MB 被清出，替换为约 2.2MB 书桌资源，首屏显著变轻。
   - 构建 27 页、链接校验全绿；白昼/夜间首页、夜间法学三栏、圆形破口切换均有浏览器截图核对。

## 九、本次（2026-09-08）变更记录

1. **3D 换模（书桌→静物）**：替换为 `still_life.glb`（用户提供）；移除 HUD 操作提示与加载遮罩（“正在载入书桌…”），改为背景先呈现、模型异步加载后淡入（canvas `is-ready` class + opacity transition）；viewer 简化为中性灯光与透明背景，不随站点明暗主题切换；源码 `desk-viewer.mjs` 已重写并 esbuild 重新打包。
2. **法学文献前言修复**：第2页前言中5个独立段落误用 `para-run`（无缩进无段距，视觉上像“突然换行”），已改为普通 `<p>`；脚注长 URL 溢出修复：`.folio-footnotes .fn` 新增 `overflow-wrap: anywhere` + `word-break: break-word`。
3. **社会保障学讲稿清理**：8月31日、9月2日两份讲稿左栏全部“AI书签”（`- **关键词**：说明` 格式的 AI 生成摘要）已清除，左栏留空；全课程内联 `####` 标题与 stray `#` 已批量修复。
4. **知识产权法讲稿修复**：9月5日讲稿人名“王美帅”“美帅”统一更正为“梅帅”（参会学生真实姓名，后续操作须注意）；“论文阅读六步法”序号错乱（标题被拆成两行、第1/5项误标为 `####` 标题）已修复为有序列表；“延伸”三条序号条目已拆为独立子条目；“文献综述技巧”下的三句话脉络已正确缩进。
5. **宪法讲稿全面修复**：9处 `####` 小标题被拼接到列表项末尾（如 `…制度保障。#### 1.3经典案例演示`）已全部移至独立行；`_【AI 注释】_` / `_【AI 潜台词挖掘】_` 下划线斜体在 CJK 上下文中渲染失效，已改为 `**【AI 注释】**：` 加粗格式；第8-11页过短（单页仅1-2行作业或 stray `#`）已合并为第8页（课后作业4条）+第9页（参考文献+待查证盲区），原12页缩为10页；全课程 stray `#` 已清除。
6. **全课程批量修复**：决策理论与方法、德语、中国法制史、民事诉讼法、土地资源管理、知识产权法、社会保障学等所有讲稿的内联 `####` 标题（共139处）、stray `#`（共123处）、AI注释渲染（共8处）已批量修复。
7. **主题切换 UI 与动效重写**：切换按钮改为纯图标（太阳/月亮），移除“暗夜/白昼”文字标签，按钮尺寸收为 2.5rem 圆形；圆形破口切换改用 **View Transitions API**（Chrome/Edge 111+）：新主题页面从按钮中心圆形扩大中**真实呈现**（而非纯色遮罩），旧主题页面在圆外保持可见；缓动改为 `cubic-bezier(0.22, 1, 0.36, 1)`（柔和先快后慢），时长 650ms；不支持 View Transitions 的浏览器降级为原纯色 veil 方案（同样使用新缓动）；两套主题 CSS 变量在首帧渲染前即已定义并应用，不存在“先变黑再出现页面”。
8. **人名备忘**：知识产权法课堂参会学生姓名为「梅帅」，非「王美帅」「美帅」，后续内容操作须注意。

## 十、本次（2026-09-10）变更记录

1. **全站文本排序改为「最新在最前」（落实用户排放习惯，见铁律 10 / §1.4）**：此前 `catalog.ts` 的 `compareNotes` 与 `bookshelf.ts` 的 `compareByTitle` 都按标题拼音排序，与用户习惯不符。
   - `content.config.ts`：schema 补 `date` 字段（dateish，可空）。
   - `catalog.ts`：新增 `noteRecencyKey()`（date → lastReadDate → finishedDate → readingDate → publishTime）与导出的 `compareNotesRecent()`（时效降序、无日期沉底、同序标题拼音兜底）；Note 增加 `date` 字段；全局 notes、合集 notes、专题 notes 全部改用该比较器。
   - `bookshelf.ts`：删除本地 `compareByTitle`/collator，文献书架每个学科分类内部与「全部」视图改用 `compareNotesRecent`。
   - `LecturesModule.astro`：搜索「相关讲稿」索引 ArticleHit 带 `date` 并改最新优先（前后端类型同步）。
   - 学科书背顺序、课程卡片拼音首字母分组等「容器导航」保持原样。
2. **效果核对**：如决策理论与方法合集为 9月3日→9月1日；社会保障学为 9月2日→8月31日；知识产权法为 9月5日→9月4日；土地资源管理为 9月4日→8月31日→（无日期）课程考核标准；脑图集为 2026-09-09 两篇→8月31日→8月21日。
3. **流程固化**：铁律新增第 10 条（排序习惯）、第 11 条（提交后自觉 push，无需询问）；§1.4 写明排序口径与三个作用点；§5.4、§六清单同步。
4. 构建：astro check 0 错、29 页构建、30 HTML 链接校验全过；浏览器实测首页两模块与合集页顺序正确、0 碎图。

## 十一、本次（2026-09-11）变更记录

1. **★新增铁律 12（最高重点，用户两次强调、曾因此返工）**：左右便签栏 L/R 只放用户本人的个人/手写笔记，严禁放 AI 生成的【提示】【思考】【核心逻辑】【本节总结】【演变逻辑链】【核心规则】等；无个人笔记时 L/R 留空，原 PDF 全部文字（含 AI 标签）并入中栏 M。§1.1、§2.2 同步加重点标注。
2. **土地资源管理《9月7日讲稿》返工为纯中栏**：上一版误把 AI 的提示/思考/核心逻辑/本节总结拆进左右栏并分带，本版删除全部 L/R 与 band，11 页内容全部并入 M，守恒校验 7381 字逐字相等、0 丢失。
3. **新增土地资源管理《我的作业一》**：用户本人课程作业（docx），纯中栏、无日期（沉底），守恒校验通过。
4. **文献笔记新增 3 篇海外耕地投资论文（用户要求自行分类）**：《东南亚地区中国海外耕地投资项目的空间分布及影响因素分析》（自然资源学报 2021）、《中国海外耕地投资发展的时空格局演变与影响因素》（资源科学 2020）、《部门合作型海外耕地投资模式分析》（农业现代化研究 2017）。三篇作者均出自公共管理学院、研究方向为土地管理与粮食安全，且隶属土地资源管理课程，故 `category` 统一显式标为「11管理学」（不依赖“投资→经济学”的标题关键词兜底）。均为原文、无个人手写笔记，故按铁律 12 只排中栏、L/R 留空。三篇 md **平铺在 `文献笔记\` 根目录、不建任何“海外耕地投资”合集**（用户明确：书架只有学科分类、没有合集）。
5. **★新增铁律 13（用户当面强调）**：文献书架只有 12 个学科分类、没有合集；同主题文献逐篇平铺、靠 `category` 归类，禁止在文献模块建子合集/子文件夹；“课程合集”是教学讲稿模块才有的形态。
6. **★三篇文献公式与英文摘要返工（用户打回，新增 §2.3）**：①论文1 公式 (1)–(3)、论文2 公式 (1)–(12) 原先用 `<sub>/<sup>`+Unicode（x̄、Σ、S²、“/”当分数线）硬拼，KaTeX 不接管、等于没渲染，本版全部改为标准 LaTeX（独立式 `$$…$$`+`\tag{n}`，行内符号 `$…$`，共 15 个独立式、91 个行内式），并用项目自带 katex 逐条 `renderToString` 试渲染 0 FAIL；②三篇英文 Abstract 原先把“标题+作者+单位+斜体摘要+关键词”塞进一个 `<p>`、正体斜体糊在一起且不换行，本版改为标题/作者/单位正体各自成段、摘要按背景-方法-结果(1)(2)(3)-结论分斜体小段、关键词独立成行；同时补回论文3英文摘要此前被漏掉的三个 “For the … mode” 整句（英文同样守恒）；③记录工具教训：PowerShell 内联 `python -c` 含 `$` 会被当变量吃掉，含 `$` 的替换一律写成 .py 文件执行。

## 十二、本次（2026-09-13）变更记录

1. **★讲稿正文开头小字全部改为正文字体（用户打回）**：此前 21 份教学讲稿在第一页中栏开头用 `>` blockquote 写课程标题/授课教师等说明行，而 `folio.css` 中中栏 blockquote 被设为 `font-size: 0.84em` 小一号灰字，导致每篇讲稿开头都有一行不协调的小字。本版批量去掉全部讲稿开头 blockquote 的 `>` 标记，改为普通正文段落（涉及法语 4 篇、英语 4 篇、德语 4 篇、土地资源管理 4 篇、社会保障学 2 篇、中国法制史 2 篇、知识产权法 1 篇，共 21 篇）。§2.2 新增规范：讲稿开头不要用 blockquote，正文中间真正需要题注/引用时才用。
2. **★英语《9月11日讲稿》封面突兀修复（用户打回）**：封面卡片 frontmatter `description` 中含 `inhabit/enormous/explorer/geographer/geographer/ephemeral/eternal` 一串斜线连接的单词，浏览器默认不在 `/` 处换行，长串单行溢出把封面卡片撑得又大又突兀，且 `geographer` 重复了两次。修复：①`global.css` 的 `.entry-card__description` 新增 `overflow-wrap: anywhere` + `word-break: break-word`，兜底所有长串换行；②该篇 description 中斜线前后加空格（`inhabit / enormous / ...`）并去掉重复的 geographer，从根源避免长串。§七故障表新增对应条目。
3. **批量修复脚本**：`_tools/fix-opening-blockquote.py`，自动扫描所有教学讲稿目录，去掉第一个 `<!--col:M-->` 之后连续的开头 blockquote，保留正文中间的引用/AI注释不动。
4. 构建验证：`npm run build` 全绿（astro check 0 错、60 页构建、61 HTML 链接校验通过）；无头浏览器截图核对英语合集页封面卡片大小一致、法语讲稿开头为正文字体。

## 十三、本次（2026-09-13）中文标点规范化变更记录（用户要求地毯式清查）

1. **★法语《9月12日讲稿》两处知识性笔误（用户指出）**：①广告缩写 `AD = étage（楼层）` 写错，法语“楼层”étage 的缩写应为 **ÉT**，正文与复习要点两处均已更正（SDB/CUI/CH/IMB/PARK 不变）；②序数词 `3e（第三）` 的 e 应为右上角小角标，改为 `3ᵉ`，并把“数字右上角标注小**a**”更正为“标注小**e**”（法语序数词上标 e，premier 例外用 er）。
2. **★中文直双引号地毯式替换（用户打回，引号“一团糟”）**：23 个文件共 **950 处**中文行文里的英文直双引号 `"` 按奇偶配对替换为中文弯引号 `“”`；脚本先剥离 frontmatter（兼容 BOM）、代码块、HTML 属性、公式，整句外文原文行（如小王子英文原文）保留半角。典型修复即用户点名的邵科论文引导问题（“中和正道”“文明之治”“人权”“国家利益”等）。
3. **★中文单引号 5 处**：`'内部人士'`、`'真理'`、`'四有'`、`'兵法'`、`'戴帽子的女人'` 改为中文单引号 `‘’`；外语撇号、音标、数学转置保留半角。
4. **其他全角化**：①邵科论文 9 处中文疑问句的英文问号 `?`→`？`（英语感叹句公式 `How+adj+主语+谓语!` 保留半角）；②中国法制史 2 处行文省略号 `...`→`……`（外语语法占位符如 `ne … pas`、`在…之后`、`not only...but also` 属教学记号，保留）；③中文行文里的半角括号改全角：邵科论文正文夹注 80 处（脚注英文文献保留半角）、法语/宪法/社保讲稿序号 `(1)(2)`→`（1）（2）`、决策理论实验选项 `(A)(B)`、英语讲稿中文夹注等；④14+19 处中文后英文逗号 `,`→`，`、并列前缀改顿号；⑤知识产权法 `（即Trips协议)` 前后括号不配对、`Sir Henry Maine(1822—1888):` 等更正。
5. **明确保留半角的场景（不要误改）**：数学/统计公式与函数、半开区间、整句外文原文与其语法标注、外语词形变化、英语音标、英文术语对照表英文列、GB/T 7714 参考文献著录与“引用格式”行、脚注内英文文献、frontmatter 与 HTML 属性。
6. **规范沉淀**：新增 §2.3-D「中文标点规范」五条 + §七故障表两行；构建验证 `npm run build` 全绿（60 页、61 HTML 链接校验通过），并在构建产物 HTML 中逐一核对用户点名的引号/问号与法语 ÉT、3ᵉ、全角序号均已生效。
